#pragma once

int screenWidth = 1280;
int screenHeight = 720;

void updateScreen()
{
    // screenWidth = GetRenderWidth();
    // screenHeight = GetScreenHeight();
    // Vector2 windowPos = GetWindowPosition(); // Get current window position
    // Vector2 windowScale = GetWindowScaleDPI(); // Get DPI scaling
    // int currentWidth = GetMonitorWidth(GetCurrentMonitor()) * windowScale.x;
    // int currentHeight = GetMonitorHeight(GetCurrentMonitor()) * windowScale.y;
};
