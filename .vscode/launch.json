{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/build/Debug/RayLibGame.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "terminal": "external",
            "preLaunchTask": "CMake: build",
        }
    ]
}
